<%= form_for(@type_test) do |f| %>
  <% if @type_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_test.errors.count, "error") %> prohibited this type_test from being saved:</h2>

      <ul>
      <% @type_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :instruction %><br>
    <%= f.text_area :instruction %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
